# 企业文档接入治理

`POST /api/enterprise/documentai/ingestion-governance` 在 OCR、向量化和字段抽取之前统一检查文件安全、来源可信、类型与大小、文档分类、个人信息处理依据、静态加密、重复件、保留期和人工审批。

接口返回 `ACCEPT`、`REVIEW` 或 `REJECT`，同时给出存储级别、有效保留期、阻断项、复核原因和后续动作。高敏文档不会因模型可识别就绕过数据治理；恶意文件或校验失败的文件会直接隔离。

该能力完全本地运行，不依赖外部模型。生产接入时可把策略结果写入审计库，并将 `REVIEW` 对接企业审批流。
